// import 'class_modifiers.dart';

// class kid
//     extends Human {} // Error: The class 'Human' can't be extended outside it library because it is marked as final.

// class Rectangle implements Shape {
//   void draw() {
//     print('The rectangle is drawn.');
//   }
// }

// class painter extends job {
//   // Error : The class 'job' can't be extended, implemented, or mixed in outside of its library because it's a sealed class.
//   painter(int salary) : super(salary);
// }
